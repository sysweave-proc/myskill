---
description: 把 SDD 任务转成测试先行的 TDD 任务对 / Convert SDD tasks into TDD task pairs
argument-hint: "[--task-group <group>]"
allowed-tools: ["Read", "Glob", "Grep", "AskUserQuestion", "TaskCreate", "TaskGet", "TaskList", "TaskUpdate"]
---

Run the **create-tdd-tasks** workflow with: $ARGUMENTS

1. **Load the skill and follow it exactly:**
   - Read `${CODEBUDDY_PLUGIN_ROOT}/skills/create-tdd-tasks/SKILL.md`
   - If that path is not resolvable, glob `**/agent-alchemy-tdd-tools/skills/create-tdd-tasks/SKILL.md`

2. **Prerequisite:** this skill transforms tasks that were already generated by **`/create-tasks`** (sdd-tools plugin, same marketplace). If no tasks exist, stop and tell the user to run `/create-tasks <spec-path>` first.

3. **`--task-group <group>`:** restrict to tasks whose `metadata.task_group` matches; otherwise process all groups you can see via `TaskList`.

4. **Output contract:** every implementation task must be preceded by a paired test task with a RED-GREEN dependency; do not reorder or delete existing tasks.
